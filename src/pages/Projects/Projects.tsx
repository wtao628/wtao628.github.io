import React from 'react';

const projects = [
  { name: 'Ferromagnetism', link: '', technologies: ['Python', 'Jupyter'] },
]

/**
 * The page containing a searchable list of projects.
 * @returns {React.JSX.Element}
 */
export default function Projects(): React.JSX.Element {
  return (
    <div>
    </div>
  )
}